# Test 004 - Export Follow-Up

Use the already approved carousel from this current conversation.

Switch to Export mode only.

Requirements:
- Do not redesign the carousel.
- Do not rewrite the carousel copy.
- Do not regenerate the preview.
- Use the approved carousel exactly as it already exists in this conversation.
- Provide the export-ready HTML.
- Provide the Playwright export script.
- Confirm that the slide count in the export script matches the actual approved carousel.
- Ensure the exported result excludes all preview-only Instagram chrome, including `.ig-header`, `.ig-dots`, `.ig-actions`, and `.ig-caption`.

Output only what is needed for Export mode.
